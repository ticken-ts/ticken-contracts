{
  "dependencies": {
    "@openzeppelin/contracts": "^4.8.1"
  },
  "scripts": {
    "abigen": "abigen --abi build/TickenEvent.abi --pkg public_blockchain --type TickenEvent --out scticket.go --bin build/TickenEvent.bin",
    "build": "solc --overwrite --base-path node_modules --bin --abi scticket.sol -o build"
  }
}
